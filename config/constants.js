const constents = Object.freeze({
    regex : /^[6-9]\d{9}$/,
    email : /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
    EMPTYFIELD : "Number Field should not ne empty",
    VALIDFORMAT : "Number Not in right Format"
})


module.exports = {constents}